*.iml
.gradle/
local.properties
.idea/
.DS_Store
Thumbs.db
build/
captures/
.externalNativeBuild/
.cxx/

# In-progress design mockups + logo source art (large; not part of the shipped app)
design/

# Private working notes (handoff logs, research, calibration) — local only, repo is public
notes/

# Release signing — PRIVATE, never commit (repo is public)
keystore/
keystore.properties
*.jks
*.keystore
